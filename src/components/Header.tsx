import popupIcon from "../assets/images/popupIcon.png";

const Header = () => {
    return (
        <header>
            <img src={popupIcon} alt="icon" />
            <h1>MSIB Bookmarker</h1>
        </header>
    );
};

export default Header;
